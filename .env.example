# Adelaide Design Week — local build secrets
#
#   cp .env.example .env     then fill in the values below
#
# .env is gitignored and never leaves this machine. Nothing here is read by
# a Cloudflare build: the map is local-only for now (see src/map.11tydata.js),
# and the geocoding and thumbnail scripts are run by hand.
#
# Tokens are made at https://account.mapbox.com/access-tokens/


# PUBLIC token — starts pk.
# Read by the map page in the browser. Default scopes are enough.
# When the map is eventually published, restrict this one to
# adelaidedesignweek.com.au on the Mapbox dashboard first.
MAPBOX_PUBLIC_TOKEN=

# SECRET token — starts sk.
# Used once, by scripts/publish-map-style.js, to upload the basemap style
# to your Mapbox account. Needs the scopes styles:write and styles:read.
# Never sent to the browser and never committed.
MAPBOX_SECRET_TOKEN=

# Only needed if the username cannot be read out of the secret token.
# MAPBOX_USERNAME=
